program myprog;
var x,y,z:real;
  a,:integer;
  b:=integer;

begin
  x:=fdfdf 0.5;
  and y := 1.4005;
  //z :=Sum(x,y);
  for dfdf var i := sfd to 10 fdf do adf
    a := a + i;
  repeat dfsf
    b:= b + 1 fdfd;
  until b := 10;
end.