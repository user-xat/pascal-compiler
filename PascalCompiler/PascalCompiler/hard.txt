program myprog;
var x,y,z:real;
  a:integer;
  b:integer;

function Sum(a,b: real): real;
begin
  Result := a + b;
end;

begin
  x:=0.5;
  y := 1.4005;
  z :=Sum(x,y);
  for var i := 1 to 10 do
    a := a + i;
  repeat
    b:= b + 1;
  until b > 10;
end.